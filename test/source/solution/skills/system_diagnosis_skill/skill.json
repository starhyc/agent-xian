{
  "name": "system_diagnosis_skill",
  "description": "Analyze system logs and form schemas to diagnose problems and identify root causes.",
  "entrypoint": "scripts/run.py",
  "timeout_seconds": 60,
  "input_schema": {
    "type": "object",
    "properties": {
      "base_dir": {
        "type": "string",
        "description": "Directory containing input files (frontend_log.log, backend_validation.log, network.har, form_schema.json)"
      }
    },
    "required": ["base_dir"],
    "additionalProperties": false
  }
}